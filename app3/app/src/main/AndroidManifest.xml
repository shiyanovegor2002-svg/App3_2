<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App3">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Service 1: Hardware-Backed Keystore with world-writable storage (M9: Insecure Data Storage) -->
        <service
            android:name=".services.VulnerableKeystoreService1"
            android:enabled="true"
            android:exported="true" />

        <!-- Service 2: KeyChain with weak password protection (M1: Improper Credential Usage) -->
        <service
            android:name=".services.VulnerableKeyChainService2"
            android:enabled="true"
            android:exported="true" />

        <!-- Service 3: Hardware-Backed Keystore with weak password (M1: Improper Credential Usage) -->
        <service
            android:name=".services.VulnerableKeystoreService3"
            android:enabled="true"
            android:exported="true" />

    </application>

</manifest>
